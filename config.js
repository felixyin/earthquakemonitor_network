'use strict'

let config = {
    // Use ESLint (extends `standard`)
    // Further changes can be made in `.eslintrc.js`
    eslint: false,

    // webpack-dev-server port
    port: 9080,

    // 服务器地址
    // server: 'http://139.224.1.36:8080'
}

module.exports = config
